version: '3.3'
services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node_1
    command: yarn debug
    ports:
      - 3000:3000
      - 9229:9229
    tty: true
    volumes:
      - ./src:/app/src
